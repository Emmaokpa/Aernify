
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
  
    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isAdmin() {
      // Check for the custom claim 'isAdmin' on the auth token
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }

    // Rules for the users collection
    match /users/{userId} {
      // A user can create their own profile.
      // The document must not exist yet.
      // weeklyCoins must be initialized.
      allow create: if isSignedIn() 
                    && isOwner(userId) 
                    && request.resource.data.isAdmin == false
                    && request.resource.data.weeklyCoins == 0
                    && !exists(/databases/$(database)/documents/users/$(userId));
      
      // Any signed-in user can read public user data and list users for the leaderboard.
      allow get, list: if isSignedIn();
      
      // TEMPORARILY RELAXED RULE: Allow an owner to update their own profile,
      // including changing their admin status. This is for a one-time admin grant.
      allow update: if isOwner(userId) || isAdmin();

      // Only admins can delete user documents
      allow delete: if isAdmin();
    }

    function incomingCoinChange() {
        // This function calculates the change in coins. It can be positive (earning) or negative (spending).
        // This prevents a user from arbitrarily setting their coin value to any number.
        return request.resource.data.coins - resource.data.coins;
    }
    
    // Rules for the daily_logins collection
    match /daily_logins/{loginId} {
        // A user can create a login record for themselves for the current day.
        // The document ID must be in the format 'userId_YYYY-MM-DD'.
        // The document should only contain the 'claimedAt' field.
        allow create: if isSignedIn() 
                      && request.resource.data.size() == 1 
                      && "claimedAt" in request.resource.data
                      && loginId.split('_')[0] == request.auth.uid;

        // Nobody can update or delete login records.
        allow update, delete: if false;

        // Only the owner can read their login records.
        allow get: if isSignedIn() && isOwner(loginId.split('_')[0]);
    }

    // Rules for the games collection
    match /games/{gameId} {
        // Any signed-in user can read game data.
        allow read: if isSignedIn();
        // Only admins can write (create, update, delete) game data.
        allow write: if isAdmin();
    }

    // Rules for the offers collection
    match /offers/{offerId} {
        // Any signed-in user can read offer data.
        allow read: if isSignedIn();
        // Only admins can write (create, update, delete) offer data.
        allow write: if isAdmin();
    }

    // Rules for the products collection
    match /products/{productId} {
        // Any signed-in user can read product data.
        allow read: if isSignedIn();
        // Only admins can write (create, update, delete) product data.
        allow write: if isAdmin();
    }

     // Rules for the giftCards collection
    match /giftCards/{giftCardId} {
        // Any signed-in user can read gift card data.
        allow read: if isSignedIn();
        // Only admins can write (create, update, delete) gift card data.
        allow write: if isAdmin();
    }

    // Rules for the offer_submissions collection
    match /offer_submissions/{submissionId} {
        // A user can create their own submission.
        allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
        
        // Admins can read all submissions. Users can read their own.
        allow read: if isAdmin() || (isSignedIn() && resource.data.userId == request.auth.uid);

        // Only admins can update the status of a submission. A user cannot change their own submission.
        allow update: if isAdmin();
        
        // Nobody can delete submissions for now.
        allow delete: if false;
    }

    // Rules for the redemption_requests collection
    match /redemption_requests/{requestId} {
      // A user can create their own request.
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;

      // Admins can read all requests. Users can read their own.
      allow read: if isAdmin() || (isSignedIn() && resource.data.userId == request.auth.uid);

      // Only admins can update the status of a request.
      allow update: if isAdmin();
      
      // Nobody can delete requests.
      allow delete: if false;
    }
    
    // Rules for the withdrawal_requests collection
    match /withdrawal_requests/{requestId} {
      // A user can create their own withdrawal request.
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;

      // Admins can read all requests. Users can read their own.
      allow read: if isAdmin() || (isSignedIn() && resource.data.userId == request.auth.uid);

      // Only admins can update the status of a request.
      allow update: if isAdmin();
      
      // Nobody can delete requests for now.
      allow delete: if false;
    }

    // Rules for the challenges collection
    match /challenges/{challengeId} {
      // Any signed-in user can read challenge definitions.
      allow read: if isSignedIn();
      // Only admins can write (create, update, delete) challenges.
      allow write: if isAdmin();
    }

    // Rules for the user_challenge_progress collection
    match /user_challenge_progress/{progressId} {
      // progressId is in format "userId_YYYY-MM-DD"
      function getUserId() {
        return progressId.split('_')[0];
      }
      
      // Users can read their own progress.
      allow get: if isSignedIn() && isOwner(getUserId());
      
      // Users can create their own progress document for today.
      allow create, update: if isSignedIn() && isOwner(getUserId());
      
      // Nobody can delete progress.
      allow delete: if false;
    }

    // Rules for the orders collection
    match /orders/{orderId} {
      // A user can create their own order.
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;

      // Admins can list all orders. Users can read their own individual order.
      allow list: if isAdmin();
      allow get: if isSignedIn() && (isAdmin() || resource.data.userId == request.auth.uid);

      // Only admins can update the status of an order.
      allow update: if isAdmin();
      
      // Nobody can delete orders for now.
      allow delete: if false;
    }
    
    // Fallback rule to deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

    